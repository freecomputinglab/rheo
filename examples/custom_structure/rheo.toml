[compile]
# Only search for .typ files in the content/ directory
content_dir = "content"

# Exclude template/library files from compilation
exclude = ["content/lib/**/*.typ", "content/_*.typ"]

[html]
# Copy static assets to HTML output using glob patterns
static_files = [
  "static/css/**",
  "static/images/**",
  "data/*.json"
]
